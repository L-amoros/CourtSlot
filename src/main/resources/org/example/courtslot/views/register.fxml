<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane alignment="CENTER" stylesheets="@../styles/styles.css" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="org.example.courtslot.controller.RegisterController">

   <Pane maxHeight="Infinity" maxWidth="Infinity" styleClass="background" />

   <VBox alignment="CENTER" maxHeight="640" maxWidth="420" prefHeight="640" prefWidth="420" spacing="14" styleClass="card">

      <!-- Cabecera -->
      <VBox alignment="CENTER" spacing="6">
         <Label style="-fx-font-size: 42px;" text="🏟️" />
         <Label styleClass="brand-title" text="CourtSlot" />
      </VBox>

      <Separator styleClass="separator" />

      <Label styleClass="title" text="Crea tu cuenta" />
      <Label maxWidth="300" styleClass="subtext" text="Únete para reservar tus pistas favoritas" wrapText="true" />

      <!-- Formulario -->
      <VBox maxWidth="300" spacing="10">
         <Label styleClass="field-label" text="Nombre completo" />
         <TextField fx:id="nombreField" accessibleText="Campo de nombre" promptText="Tu nombre" styleClass="input-field" />

         <Label styleClass="field-label" text="Email" />
         <TextField fx:id="emailField" accessibleText="Campo de email" promptText="tu@email.com" styleClass="input-field" />

         <Label styleClass="field-label" text="Contraseña" />
         <PasswordField fx:id="passwordField" accessibleText="Campo de contraseña (mínimo 6 caracteres)" styleClass="input-field" />

         <Label styleClass="field-label" text="Confirma tu contraseña" />
         <PasswordField fx:id="confirmPasswordField" accessibleText="Repite la contraseña" styleClass="input-field" />
      </VBox>

      <!-- Feedback -->
      <Label fx:id="errorLabel" maxWidth="300" styleClass="error-label" wrapText="true" />

      <Button fx:id="registerBtn" accessibleText="Botón para crear cuenta nueva" maxWidth="300" onAction="#onLoginButtonClick" prefHeight="44" styleClass="btn-primary" text="Crear cuenta" />

      <HBox alignment="CENTER" spacing="5">
         <Label styleClass="subtext" text="¿Ya tienes cuenta?" />
         <Hyperlink accessibleText="Ir a la página de inicio de sesión" onAction="#onLoginButtonClick" styleClass="link" text="Inicia sesión" />
      </HBox>
   </VBox>

</StackPane>
